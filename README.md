# Invoice Corrector API

This is a FastAPI backend that accepts two uploaded invoice images and returns a corrected invoice PDF.

## Run locally

```bash
pip install -r requirements.txt
uvicorn main:app --reload

